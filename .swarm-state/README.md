# .swarm-state/

This directory is the designated output location for all AppForge autonomous agent runtime artifacts, logs, memory dumps, debug output, build logs, and run state files.

- All agent output MUST be written here, not to the repo root.
- This directory is gitignored except for `.gitkeep` and this README.
- Human contributors should never write files here manually.
