# Development Dockerfile with hot reload
FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

# Expose ports
EXPOSE 5000 5001

# Start with nodemon for hot reload
CMD ["npm", "run", "dev"]
