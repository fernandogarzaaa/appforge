name: Autonomous Swarm Cycle (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: |
          echo "autonomous_swarm.yml is deprecated."
          echo "Use .github/workflows/swarm-loop.yml as the single swarm controller."
